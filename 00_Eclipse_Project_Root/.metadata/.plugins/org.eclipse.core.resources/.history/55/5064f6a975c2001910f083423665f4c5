(function(){
    'use strict' ;
    app
    .controller('AboutUsController', AboutUsController);

    // inject dependencies
    AboutUsController.$inject = ['$location', 'UserService','$localStorage'] ;

    function AboutUsController(adminService, $location,$localStorage) {
    	
    	$timeout(alert("Welcome to About Us section: Who we are..."), 1000);
    }
})();
